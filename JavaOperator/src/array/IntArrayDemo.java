package array;

import java.util.Scanner;

public class IntArrayDemo {
	public static void main(String[] args) {
		int[] arr;	//숫자형 배열의 선언
		arr = new int[10000];
		
		int[] arr2 = new int[10000];
		
		Scanner scanner = new Scanner(System.in);
		/*
		 * 자바는 객체지향 언어이다.
		 * 이 말은 프로그램 모양을 객체화 시켜서 사용한다는 뜻과 동일
		 * 객체화 시킨다 -> main()메소드를 해체하고 단 하나의 main()만 
		 * 프로그램에 존재시키고 나머지는 클래스화 하여  필요로할 때에만 
		 * 그 기능을 호출하여 사용한다는 의미이다.
		 * 
		 * 호출방법 -> 클래스이름 객체이름 = new 클래스이름()
		 * ()안에 있는 변수는 파라미터라고 하는데
		 * 필요에 따라 있을 수도 있고 없을 수도 있다. 
		 * 단, 클래스 이름, 객체이름, new, 클래스이름()는 반드시 필요하다.
		 * 그리고 이때 객체이름을 참조변수라고 한다.
		 * */
		
	}
}
